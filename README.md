# FinTrack - Formação em React

This is a financial control project developed during the React module of the **Formação em React** course at the **Full-Stack Club** platform. The project's goal is to apply advanced React concepts, TypeScript, and API integration.

## 🚀 Technologies

- **React 19** (Client-side SPA)
- **TypeScript**
- **Vite**
- **Tailwind CSS**
- **Lucide React** (Icons)
- **Date-fns** (Date management)

## 🛠️ Features

- Date range selection for financial filtering.
- Dynamic UI components (Field, Separator, DatePickerWithRange).
- State synchronization with URL query parameters.
- Fully responsive interface.

## ⚙️ How to run

1.  Clone the repository:
    ```bash
    git clone https://github.com/adrjmiranda/fin-track.git
    ```
2.  Install dependencies:
    ```bash
    npm install
    ```
3.  Run the development server:
    ```bash
    npm run dev
    ```

## 📝 Commit Standards

This project uses **Husky** and **Commitlint** to enforce Conventional Commits.

- Header limit: 100 characters.
- Format: `type: description` (all lowercase, no period at the end).

---

Developed by [Adriano Miranda](https://github.com/adrjmiranda) during the Full-Stack Club journey.
