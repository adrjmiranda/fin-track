import Header from '@/components/common/Header/inde';

const Home = () => {
	return (
		<main>
			<Header />

			<div className='flex items-center justify-between'>
				<h2>Dashboard</h2>
			</div>
		</main>
	);
};

export default Home;
